import {
  horizontalScale,
  moderateScale,
  scaleFont,
  verticalScale,
} from './functions';

import iconName from './iconName.json';
import userInfo from './userInfo.json';

export {
  horizontalScale,
  iconName,
  moderateScale,
  scaleFont,
  userInfo,
  verticalScale,
};
